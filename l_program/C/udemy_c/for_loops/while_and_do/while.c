#include <stdio.h>

int main(){
    int contador = 1;
    while (contador <= 5){
        printf("Contando: %d\n", contador);
        contador++;
    }
    printf("Contagem regressiva para a exterminacao total: \n");
    return 0;
}