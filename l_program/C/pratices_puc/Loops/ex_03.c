/*Elabore um programa que leia vários números inteiros até o usuário digitar um número negativo. Para cada
número lido deverá ser calculado e impresso seu fatorial. Faça com FOR e WHILE. Dica:
o Fatorial de num é uma sequência de multiplicações de num até 1.
o Utilize um loop que vai de 1 a num (ou de num a 1) e que vai atualizando essa multiplicação termo a termo em uma variável
int.
*/
#include <stdio.h>
#include <stdbool.h>
int main() {
    int n;
    printf("Digite n: ");
    scanf("%d", &n);
    return 0;
}